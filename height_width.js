// width/height - window or any element
//innerHeight-- window
// innerWidth-- window
// getBoundingClientRect()-any element
/*console.log('heigth', window.innerHeight)
console.log('width',window.innerWidth)
const btn = document.querySelector('.btn')
btn.addEventListener('click',()=>{
  const dim = btn.getBoundingClientRect()
  console.log(dim)
})*/